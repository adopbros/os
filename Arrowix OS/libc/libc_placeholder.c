/*
 * Placeholder translation unit for the minimal C library.
 *
 * The freestanding libc (string, stdio, stdlib) is introduced in Phase 7.
 * This file only exists so the library is valid before any source is added.
 */

int arrowix_libc_placeholder(void)
{
    return 0;
}
