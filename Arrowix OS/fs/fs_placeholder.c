/*
 * Placeholder translation unit for the filesystem subsystem.
 *
 * The VFS layer, ramfs, and FAT32 are introduced in Phase 6. This file only
 * exists so the OBJECT library is valid before any fs source is added; remove
 * it once a real filesystem lands.
 */

int arrowix_fs_placeholder(void)
{
    return 0;
}
